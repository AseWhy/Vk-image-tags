import { VI_IMG_TAGS_SEL_CONT_TRIGGER, VK_IMG_TAGS_CONT_CONT_TRIGGER } from "../../../../../lib/contants";
import select from "./select";
import view from "./view";

export default new Map([
    [ VI_IMG_TAGS_SEL_CONT_TRIGGER, select ],
    [ VK_IMG_TAGS_CONT_CONT_TRIGGER, view ],
])